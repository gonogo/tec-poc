<%#
  TEC PCN — Roles and access tab (CCD shell placeholder).
%>
<% caption = (defined?(caption) && caption) ? caption : "TEC PCN — Roles and access" %>
<% body = [
  partial("partials/blockframe/text", locals: { size: :title, label: "Roles and access" }),
  partial("partials/blockframe/text", locals: { size: :line, label: "Case access is managed separately from Work Allocation in this PoC." }),
  partial("partials/blockframe/text", locals: { size: :medium, label: "Allocated users" }),
  partial("partials/blockframe/text", locals: { size: :short, label: "Access categories" }),
  '<span class="bf-action" title="Allocate access (placeholder)"></span>'
].join %>
<%= partial "partials/blockframe/page", locals: { caption: caption, active: :roles, body: body } %>
